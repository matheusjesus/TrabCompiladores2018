PROGRAM p03
BEGIN
  --funcao fatorial recursiva
  FUNCTION INT fatorial ( INT num ) BEGIN
    INT fat;

    IF ( num <= 1 ) THEN
        RETURN 1;
    ELSE
        fat = n * fatorial(num - 1);
        RETURN fat;
    ENDIF
  END

  --funcao main
  FUNCTION VOID main ( ) BEGIN
    STRING comeco := "Insira um numero: ";
    STRING fim := "O fatorial é ";
    INT num;
    INT f;

    WRITE ( comeco );
    READ ( num );

    f := fatorial( num );

    WRITE ( fim );
    WRITE ( num );
  END

END
